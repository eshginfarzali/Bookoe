// import {useSelector,useDispatch} from "react-redux"
// import {increment, decrement, reset, type RootState, incrementByAmount} from './redux'

function App() {
  // const {count} = useSelector((root:RootState)=>root.counter)
  //  const dispatch = useDispatch()
  return (
    <>
    {/* <h2>count: {count}</h2>
    <button onClick={()=>
    dispatch(increment())
    }
    >increment</button>
    <button
     onClick={()=>
      dispatch(decrement())
      }
    >decrement</button>
    <button
    onClick={()=>{
      dispatch(reset())
    }}
    >reset</button>
<input type="number" onBlur={({target:{ value}})=>{
  dispatch(incrementByAmount(Number(value)))
}} /> */}
    </>
  )
}

export default App
